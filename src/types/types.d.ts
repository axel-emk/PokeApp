export type Pokemon = {
  name: string;
  id: string;  
  imgSrc: string;
};

export type PokemonDetails = {
  types: string;
  name: string;
  id: string;
  imgSrc: string;
  hp: number;
  attack: number;
  defense: number;
};
